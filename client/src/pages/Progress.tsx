import { trpc } from "@/lib/trpc";
import { FlashcardDeck } from "@/components/FlashcardDeck";
import { BarChart3, BookOpenCheck, Flame, Sparkles, Target } from "lucide-react";
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis } from "recharts";

export default function Progress() {
  const overview = trpc.progress.overview.useQuery();
  const data = overview.data;
  const stats = [
    { label: "Current streak", value: `${data?.streak ?? 0} days`, icon: Flame, tone: "bg-[#FDE8DF] text-[#BB5135]" },
    { label: "Completed sessions", value: data?.completedSessions ?? 0, icon: BookOpenCheck, tone: "bg-[#E4F0F4] text-[#34718A]" },
    { label: "Vocabulary learned", value: data?.vocabularyLearned ?? 0, icon: Sparkles, tone: "bg-[#E9EFE3] text-[#57744B]" },
  ];
  return <div className="space-y-6"><section className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-accent">Learning record</p><h1 className="editorial-title mt-2 text-4xl font-semibold">Your progress, in practice.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">These signals are designed to help you notice patterns and keep momentum. They are not an official proficiency score.</p></div><span className="flex size-12 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><BarChart3 className="size-5" /></span></section><section className="grid gap-4 md:grid-cols-3">{stats.map(item => { const Icon = item.icon; return <div key={item.label} className="study-card p-5"><span className={`inline-flex rounded-xl p-2.5 ${item.tone}`}><Icon className="size-5" /></span><p className="mt-5 text-xs font-bold uppercase tracking-[0.13em] text-muted-foreground">{item.label}</p><p className="editorial-title mt-2 text-3xl font-semibold">{item.value}</p></div>; })}</section><FlashcardDeck /><section className="grid gap-6 lg:grid-cols-[1.45fr_0.85fr]"><div className="study-card p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-muted-foreground">Common learning patterns</p><h2 className="editorial-title mt-2 text-3xl font-semibold">Where to focus next</h2></div><Target className="size-5 text-accent" /></div>{data?.errorCategories?.length ? <div className="mt-6 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.errorCategories} margin={{ top: 10, right: 0, left: -22, bottom: 0 }}><XAxis dataKey="name" tickLine={false} axisLine={false} tick={{ fontSize: 11, fill: "#697084" }} /><Tooltip cursor={{ fill: "rgba(24,34,56,0.04)" }} contentStyle={{ borderRadius: 12, border: "1px solid #E6DDD0", fontSize: 12 }} /><Bar dataKey="value" fill="#E66E4B" radius={[7, 7, 0, 0]} /></BarChart></ResponsiveContainer></div> : <div className="mt-6 rounded-2xl border border-dashed border-border p-8 text-center"><p className="text-sm font-semibold">No patterns to show yet.</p><p className="mx-auto mt-2 max-w-sm text-xs leading-5 text-muted-foreground">Finish a practice activity to start seeing the categories that deserve extra attention.</p></div>}</div><div className="study-card p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[0.14em] text-muted-foreground">Goal rhythm</p><div className="mt-5 rounded-2xl bg-[#FDE8DF] p-5"><p className="text-sm font-semibold text-primary">Today’s practice</p><p className="editorial-title mt-2 text-5xl font-semibold text-primary">{data?.todaySessions ?? 0}<span className="ml-1 text-xl text-muted-foreground">/ {data?.dailyGoal ?? 1}</span></p><p className="mt-3 text-xs leading-5 text-[#5A3F35]">Keep the goal deliberately small. A short, high-quality session counts.</p></div><div className="mt-5 space-y-3">{data?.recentActivity?.length ? data.recentActivity.map(item => <div key={item.id} className="flex items-center gap-3"><span className="size-2 rounded-full bg-accent" /><div className="min-w-0"><p className="truncate text-sm font-medium">{item.title}</p><p className="text-xs text-muted-foreground">{new Date(item.date).toLocaleDateString(undefined, { month: "short", day: "numeric" })}</p></div></div>) : <p className="text-sm leading-6 text-muted-foreground">Your completed practice, daily challenge, and career scenarios will appear here.</p>}</div></div></section></div>;
}
